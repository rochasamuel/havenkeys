{
  "name": "@havenkeys/protocol",
  "private": true,
  "version": "0.4.2",
  "type": "module",
  "description": "HavenKeys bridge protocol: message types and strict validators (mirrors crates/havenkeys-protocol)",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
